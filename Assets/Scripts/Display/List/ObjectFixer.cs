using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Display.Production;

public class ObjectFixer : MonoBehaviour
{
    private Collider m_Collider;
    private Vector3 m_Size;
    public void OnClick()
    {
        private float scalex = ProductionManager.selectedGameObjects[GlobalVariables.workNumber].transform.localScale.x;
        private float scaley = ProductionManager.selectedGameObjects[GlobalVariables.workNumber].transform.localScale.y;
        private float scalez = ProductionManager.selectedGameObjects[GlobalVariables.workNumber].transform.localScale.z;

        private float positionx = ProductionManager.selectedGameObjects[GlobalVariables.workNumber].transform.position.x;
        private float positiony = ProductionManager.selectedGameObjects[GlobalVariables.workNumber].transform.position.y;
        private float positionz = ProductionManager.selectedGameObjects[GlobalVariables.workNumber].transform.position.z;

        

    }
    
       
}
